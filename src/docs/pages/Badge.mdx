import { Badge } from '@/components/Badge';
import { ComponentPreview } from '../components/ComponentPreview';
import { PropsTable } from '../components/PropsTable';
import { PageHeader } from '../components/PageHeader';

<PageHeader title="Badge" description="Status indicators for labels, counts, and metadata. Semantic colors, three sizes, and loading state." />

## Variants

Color-coded badges for different statuses.

<ComponentPreview code={`<Badge variant="primary">New</Badge>
<Badge variant="success">Online</Badge>
<Badge variant="warning">Pending</Badge>
<Badge variant="danger">Critical</Badge>`}>
  <div className="flex flex-wrap gap-3 items-center">
    <Badge variant="primary">New</Badge>
    <Badge variant="success">Online</Badge>
    <Badge variant="warning">Pending</Badge>
    <Badge variant="danger">Critical</Badge>
  </div>
</ComponentPreview>

## Sizes

<ComponentPreview code={`<Badge size="small">Small</Badge>
<Badge size="medium">Medium</Badge>
<Badge size="large">Large</Badge>`}>
  <div className="flex items-center gap-3">
    <Badge size="small">Small</Badge>
    <Badge size="medium">Medium</Badge>
    <Badge size="large">Large</Badge>
  </div>
</ComponentPreview>

## Outline & Loading

<ComponentPreview code={`<Badge variant="outline">Draft</Badge>
<Badge isLoading>Loading</Badge>`}>
  <div className="flex gap-3 items-center">
    <Badge variant="outline">Draft</Badge>
    <Badge variant="outline">Archived</Badge>
    <Badge isLoading>Loading</Badge>
  </div>
</ComponentPreview>

## Props
<PropsTable props={[
  { name: 'variant', type: '"primary" | "success" | "danger" | "warning" | "outline"', defaultValue: '"primary"', description: 'Color and style.' },
  { name: 'size', type: '"small" | "medium" | "large"', defaultValue: '"medium"', description: 'Badge dimensions.' },
  { name: 'isLoading', type: 'boolean', defaultValue: 'false', description: 'Shows a skeleton loader.' },
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
]} />
